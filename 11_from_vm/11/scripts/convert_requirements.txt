# One-time conversion venv — NOT the submission runtime.
# python -m venv .venv_convert && source .venv_convert/bin/activate
# pip install -r scripts/convert_requirements.txt

tensorflow-cpu==2.15.1
ml-dtypes~=0.3.1
onnx>=1.14,<1.17
tf2onnx>=1.16
onnxruntime>=1.17
numpy
